import styled from "styled-components"

export default function NavBar(){
    return(
        <NavBarContainer>
        
        </NavBarContainer>
    )
}

const NavBarContainer = styled.div`
    
`;